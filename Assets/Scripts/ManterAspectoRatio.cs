using UnityEngine;
using System.Collections;

[ExecuteInEditMode]
public class ManterAspectoRatio : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
